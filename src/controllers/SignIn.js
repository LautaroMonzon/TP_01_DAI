/*import { Router } from 'express';
import { getSignedToken } from '../common/jwt.strategy.js';


const router = Router();


router.get('', async (req, res) => {
    
    const token=  getSignedToken();
    console.log(token);
    return res.status(200).json(token);

});

export default router;*/